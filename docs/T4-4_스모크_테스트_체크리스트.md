# T4-4. 스모크 테스트 체크리스트

> 설계 버전: 1.6 | 최종 수정: 2026-03-25 | 관련 CR: CR-001, CR-006, CR-007, CR-011, CR-015, CR-016, CR-017
> 단계: 6. Deployment | 배포 후 핵심 기능 생존 확인

> **프로젝트**: Aimbase
> **작성일**: 2026-03-10 (역설계)
> **원칙**: 배포 후 20분 이내, 26개 항목으로 핵심 생존 확인

---

## 배포 정보

| 항목 | 값 |
|------|-----|
| 환경 | staging / production |
| 배포 시각 | YYYY-MM-DD HH:mm |
| 버전 | v1.0.0 |
| 배포 방식 | Docker Compose |
| 실행자 | |

---

## 스모크 테스트 체크리스트

### 영역 1: 인프라 생존 (1-2분)

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 1 | API 서버 기동 | `curl http://localhost:8080/actuator/health` | `{"status":"UP"}` | ✅ | |
| 2 | DB 연결 | `curl http://localhost:8080/actuator/health` | components.db.status=UP | ✅ | |
| 3 | Redis 연결 | `redis-cli -h localhost -p 6379 ping` | PONG | ✅ | |

### 영역 2: 인증 흐름 (1-2분)

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 4 | Swagger UI 접근 | 브라우저 http://localhost:8080/swagger-ui.html | 페이지 로드 | ✅ | |
| 5 | FE 페이지 로드 | 브라우저 http://localhost:3000 | Dashboard 표시 | ✅ | |

### 영역 3: 핵심 비즈니스 흐름 (3-5분)

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 6 | 채팅 API 정상 | `curl -X POST localhost:8080/api/v1/chat/completions -H "Content-Type:application/json" -d '{"model":"gpt-4o","messages":[{"role":"user","content":"hi"}]}'` | success=true, content 반환 | ✅ | |
| 7 | 연결 목록 조회 | `curl localhost:8080/api/v1/connections` | success=true, 200 OK | ✅ | |
| 8 | 대시보드 통계 | `curl localhost:8080/api/v1/admin/dashboard` | success=true, 통계 반환 | ✅ | |
| 9 | 테넌트 목록 (Platform) | `curl localhost:8080/api/v1/platform/tenants` | success=true, 목록 반환 | ✅ | |
| 10 | FE 네비게이션 | 각 메뉴 클릭 | 에러 없이 페이지 전환 | ✅ | |

### 영역 4: RAG A++ 기능 생존 (3-5분) — CR-011

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 11 | Python 사이드카 기동 | `curl http://localhost:8100/health` | status=ok | ✅ | MCP SSE 서버 |
| 12 | RAG 검색 API | `curl -X POST localhost:8080/api/v1/chat/completions -H "Content-Type:application/json" -d '{"model":"gpt-4o","messages":[{"role":"user","content":"테스트 질문"}],"source_id":"test"}'` | RAG 컨텍스트 포함 응답 | ✅ | |
| 13 | RAG 평가 페이지 로드 | 브라우저 http://localhost:3000 → "RAG 평가" 메뉴 | 평가 대시보드 표시 | ✅ | |
| 14 | 청킹 전략 드롭다운 | Knowledge Sources 페이지 → 소스 카드 | 5가지 전략 선택 가능 | ✅ | |
| 15 | TTS API | `curl -X POST localhost:8080/api/v1/speech/tts -H "Content-Type:application/json" -d '{"text":"안녕하세요"}'` | audio/mpeg 바이너리 또는 503 (연결 없을 시) | ✅ | |
| 16 | STT API 엔드포인트 | `curl -X POST localhost:8080/api/v1/speech/stt -F "file=@test.wav"` | 200 또는 503 | ✅ | |

### 영역 5: 도구 선택 제어 기능 (1-2분) — CR-006

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 17 | tool_choice=none 채팅 | `curl -X POST localhost:8080/api/v1/chat/completions -H "Content-Type:application/json" -d '{"model":"gpt-4o","messages":[{"role":"user","content":"날씨 알려줘"}],"tool_choice":"none"}'` | 도구 호출 없이 텍스트 응답 | ✅ | |
| 18 | allowedTools 필터링 | `curl -X POST localhost:8080/api/v1/chat/completions -H "Content-Type:application/json" -d '{"model":"gpt-4o","messages":[{"role":"user","content":"hi"}],"tool_filter":{"allowed_tools":["calculator"]}}'` | 200 OK, 필터 적용 | ✅ | |

### 영역 6: 구조화된 출력 기능 (1-2분) — CR-007

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 19 | 구조화 응답 API | `curl -X POST localhost:8080/api/v1/chat/completions -H "Content-Type:application/json" -d '{"model":"gpt-4o","messages":[{"role":"user","content":"분류해줘"}],"response_format":{"type":"json_schema","json_schema":{"name":"test","schema":{"type":"object","properties":{"category":{"type":"string"}}}}}}'` | content.type=structured, JSON 응답 | ✅ | |
| 20 | response_format 미지정 하위호환 | `curl -X POST localhost:8080/api/v1/chat/completions -H "Content-Type:application/json" -d '{"model":"gpt-4o","messages":[{"role":"user","content":"hi"}]}'` | content.type=text, 기존 텍스트 응답 | ✅ | |

### 영역 7: 정책 엔진 Rule Type (1-2분) — CR-015

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 21 | 정책 목록 조회 | `curl localhost:8080/api/v1/policies` | success=true, 정책 목록 반환 | ✅ | |
| 22 | 정책 생성 (content_filter) | `curl -X POST localhost:8080/api/v1/policies -H "Content-Type:application/json" -d '{"name":"smoke-test","action":"DENY","rules":[{"type":"content_filter","keywords":["test-block"]}],"priority":1}'` | 201 Created | ✅ | |

### 영역 8: RAG 평가 LLM Judge (1분) — CR-016

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 23 | RAG 평가 실행 (fast 모드) | `curl -X POST localhost:8080/api/v1/evaluations/rag-quality -H "Content-Type:application/json" -d '{"source_id":"test","test_set":[{"question":"테스트","expected":"답변"}],"mode":"fast"}'` | 200 OK, evaluation_id 반환 | ✅ | |
| 24 | RAG 평가 페이지 로드 | 브라우저 http://localhost:3000 → "RAG 평가" 메뉴 | Fast/Accurate 모드 토글 표시 | ✅ | |

### 영역 9: 워크플로우 ConfigPanel (1-2분) — CR-017

| No. | 점검항목 | 점검 명령어 | 예상결과 | 결과 | 비고 |
|-----|---------|-----------|---------|------|------|
| 25 | 워크플로우 스튜디오 ConfigPanel | 브라우저 → 워크플로우 → 새 워크플로우 → LLM 노드 클릭 | connection_id, model, prompt 등 설정 필드 표시 | ✅ | |
| 26 | 워크플로우 목록 조회 API | `curl localhost:8080/api/v1/workflows` | success=true, steps 배열에 name 필드 포함 | ✅ | |

---

## 롤백 판단 기준

| 조건 | 판단 |
|------|------|
| No.1~3 중 1개 이상 실패 | 즉시 롤백 |
| No.6 (채팅 API) 실패 | 즉시 롤백 |
| No.4~5 실패 | 원인 분석 후 판단 |
| No.7~10 중 2개 이상 실패 | 원인 분석 후 판단 |
| No.11 (Python 사이드카) 실패 | RAG A++ 기능 제한적 — Java Fallback 확인 후 판단 |
| No.12~16 중 3개 이상 실패 | 원인 분석 후 판단 |
| No.17~18 (도구 선택 제어) 실패 | 원인 분석 후 판단 — CR-006 기능 제한 |
| No.19~20 (구조화 출력) 실패 | 원인 분석 후 판단 — CR-007 기능 제한 |
| No.21~22 (정책 Rule Type) 실패 | 원인 분석 후 판단 — CR-015 기능 제한 |
| No.23~24 (RAG LLM Judge) 실패 | 원인 분석 후 판단 — CR-016 기능 제한 |
| No.25~26 (워크플로우 ConfigPanel) 실패 | 원인 분석 후 판단 — CR-017 기능 제한 |

---

## 종합 판정

| 항목 | 값 |
|------|-----|
| 총 항목 | 26 |
| PASS | /26 |
| FAIL | /26 |
| 종합 판정 | PASS / FAIL / 롤백 |
| 판정 시각 | |
| 판정자 | |

---

## 스모크 테스트 계정

| 환경 | URL | 비고 |
|------|-----|------|
| Local | http://localhost:8080 (BE), http://localhost:3000 (FE) | Docker Compose |
| Staging | TBD | |
| Production | TBD | |
