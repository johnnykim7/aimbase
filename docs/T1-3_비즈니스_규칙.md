# T1-3. 비즈니스 규칙 정의서

> 설계 버전: 4.2 | 최종 수정: 2026-04-07 | 관련 CR: CR-002, CR-006, CR-007, CR-011, CR-012, CR-015, CR-016, CR-017, CR-020, CR-021, CR-031, CR-032, CR-033, CR-035, CR-036, CR-038

> **프로젝트**: Aimbase
> **작성일**: 2026-03-10 (역설계)
> **원칙**: 비즈니스 규칙은 코드에 고정(hardcode)되며, 변경 시 코드 수정이 필요하다.

---

## 규칙 목록

| 규칙ID | 영역 | 규칙 | 상세설명/예외 | 우선순위 |
|--------|------|------|-------------|----------|
| BIZ-001 | 도구호출 | 도구 호출 루프는 최대 5회까지만 반복한다 | ToolCallHandler에서 maxIterations=5 하드코딩. 5회 초과 시 마지막 응답을 그대로 반환 | High |
| BIZ-002 | 세션 | 세션 메시지는 Redis에 24시간 TTL로 저장한다 | TTL 만료 후 세션 자동 소멸. 키 패턴: `tenant:{tenantId}:session:messages:{sessionId}` | High |
| BIZ-003 | 멀티테넌시 | 테넌트 간 데이터는 물리적으로 격리한다 (Database-per-Tenant) | 각 테넌트는 독립된 PostgreSQL 데이터베이스를 가짐. TenantRoutingDataSource로 라우팅 | High |
| BIZ-004 | 멀티테넌시 | 테넌트 컨텍스트는 요청 단위로 ThreadLocal에 바인딩한다 | TenantResolver 인터셉터가 요청 시작 시 설정, 종료 시 클리어 | High |
| BIZ-005 | 정책 | 정책 평가는 priority 내림차순으로 수행하며, 첫 번째 DENY 또는 REQUIRE_APPROVAL에서 즉시 중단한다 | ALLOW, LOG, TRANSFORM은 계속 평가. 여러 TRANSFORM 적용 가능 | High |
| BIZ-006 | 정책 | PII 마스킹은 TRANSFORM 정책 타입으로 처리하며, 마스킹된 데이터를 transforms 맵에 저장한다 | **v2.0: Python Presidio MCP Server로 위임 (CR-002)**. 원본은 감사 로그에 기록 | Medium |
| BIZ-007 | 정책 | Rate Limiting은 Redis 기반 슬라이딩 윈도우로 구현한다 | RateLimiter 클래스. 정책 rule.config에서 윈도우/제한 설정 | Medium |
| BIZ-008 | API키 | API 키는 BCrypt로 해시하여 저장하고, 평문은 생성 시 1회만 반환한다 | 재생성 시 기존 키 무효화. 복구 불가 | High |
| BIZ-009 | 워크플로우 | 워크플로우 스텝은 위상 정렬(Kahn 알고리즘)에 따라 실행한다 | dependsOn 필드로 DAG 정의. 순환 의존 시 실행 거부 | High |
| BIZ-010 | 워크플로우 | HUMAN_INPUT 스텝에서 워크플로우는 일시정지하고 PendingApproval을 생성한다 | 승인/거부 시 resume() 호출로 실행 재개 | High |
| BIZ-011 | 워크플로우 | 스텝 실패 시 errorHandling 설정에 따라 재시도한다 | retryMaxAttempts, retryDelayMs (지수 백오프) | Medium |
| BIZ-012 | RAG | 임베딩 모델은 설정 가능하며, 기본값은 BAAI/bge-m3 (1024차원)이다. OpenAI text-embedding-3-small (1536d), KoSimCSE (768d)는 옵션으로 지원한다 | **v2.0: Python sentence-transformers로 이관 (CR-002)**. 환경변수 `DEFAULT_EMBED_MODEL`로 모델 교체 가능. 모델에 따라 차원 가변. pgvector vector 타입 | High |
| BIZ-013 | RAG | 벡터 검색은 하이브리드 방식(BM25 + 코사인 유사도)과 리랭킹을 지원한다 | **v2.0: Python RAG Pipeline으로 이관 (CR-002)**. HNSW 인덱스 유지, cross-encoder 리랭킹 추가 | High |
| BIZ-014 | 테넌트 | 테넌트 프로비저닝은 6단계를 순차 실행한다 | Master 등록 → DB 생성 → pgvector 설치 → Flyway 마이그레이션 → 시딩 → 구독 생성 | High |
| BIZ-015 | 테넌트 | 테넌트 삭제 시 DROP DATABASE를 실행하며 복구 불가하다 | 활성 연결 종료 → DROP DB → Master 삭제 → DS 캐시 제거 | High |
| BIZ-016 | LLM | LLM 어댑터는 통일된 UnifiedMessage 형식으로 프로바이더 간 메시지를 변환한다 | Role: SYSTEM, USER, ASSISTANT, TOOL_RESULT. ContentBlock: Text, ToolUse, ToolResult | High |
| BIZ-017 | LLM | LLM 응답의 FinishReason이 TOOL_USE이면 도구 호출 루프를 계속한다 | END, MAX_TOKENS, ERROR 시 루프 종료 | High |
| BIZ-018 | 프롬프트 | 프롬프트는 복합키(id, version)로 관리하며, 동일 id에 여러 버전이 공존한다 | isActive 플래그로 현재 활성 버전 구분 | Medium |
| BIZ-019 | 스키마 | 스키마는 복합키(id, version)로 관리한다 | JSON Schema Draft 7 기준. networknt validator 사용 | Medium |
| BIZ-020 | 감사 | 모든 LLM 호출, 도구 사용, 액션 실행은 감사 로그에 기록한다 | AuditLogger가 event, userId, policyDecision, details 기록 | High |
| BIZ-021 | 응답 | 모든 API 응답은 ApiResponse<T> 래퍼로 통일한다 | {success, data, error, pagination} 구조 | Medium |
| BIZ-022 | MCP | MCP 도구는 서버 연결 시 자동 탐색되어 ToolRegistry에 동적 등록된다 | MCPServerManager가 autoStart=true인 서버를 앱 시작 시 자동 연결 | Medium |
| BIZ-023 | 액션 | 액션은 Virtual Thread로 병렬 실행한다 | ActionExecutor에서 targets를 병렬 처리 | Medium |
| BIZ-024 | 플랫폼 | /api/v1/platform/** 엔드포인트는 Master DB에만 접근한다 | TenantResolver에서 해당 경로 스킵 | High |
| BIZ-025 | RAG | Contextual Retrieval 시 청크당 LLM 호출 1회로 맥락 프리픽스를 생성한다 | 문서 전문 + 개별 청크를 LLM에 전달, 100K 토큰 이하 문서만 지원. 배치 20개씩 처리 | High |
| BIZ-026 | RAG | Parent-Child 검색에서 Child 매칭 → Parent 반환 시 Parent 중복을 제거한다 | 동일 Parent에 매핑된 여러 Child 중 최고 점수 기준 정렬. Parent가 없는 청크는 기존 방식 반환 | High |
| BIZ-027 | RAG | 시맨틱 캐시 히트 판정 임계값은 코사인 유사도 0.95이다 | TTL 기본 1시간. 인제스션 시 해당 source_id 캐시 전체 무효화 | Medium |
| BIZ-028 | RAG | Citation은 컨텍스트 청크 순서 기반 번호를 부여하고, LLM에 인용 지시 프롬프트를 주입한다 | LLM이 인용을 정확히 따르지 않을 수 있음 (최선 노력). 스트리밍 시 citations는 최종 응답에 포함 | High |
| BIZ-029 | RAG | 증분 인제스션은 문서 content의 SHA-256 해시를 비교하여 변경분만 재처리한다 | 청킹 설정 변경 시 전체 재인제스션 강제. 해시 미존재 기존 문서는 최초 sync 시 해시 역산 | Medium |
| BIZ-030 | RAG | RAGAS 평가에서 ground_truth 미제공 시 Context Precision/Recall 메트릭을 생략한다 | Faithfulness, Context Relevancy, Answer Relevancy 3개는 ground_truth 없이 산출 가능 | Medium |
| BIZ-031 | 도구호출 | tool_choice 파라미터로 도구 사용 전략을 제어한다 (auto\|none\|required\|{tool_name}) | auto=기본, none=도구 미사용, required=반드시 도구 사용, {tool_name}=해당 도구 강제. Ollama는 미지원 시 auto 폴백 | High |
| BIZ-032 | 도구호출 | ToolFilterContext로 LLM에 노출할 도구를 필터링한다 | allowedTools, excludeTools, tags 3가지 필터 조건. isToolAllowed()로 개별 판정, filterTools()로 일괄 필터링. 필터 null 시 전체 노출 | High |
| BIZ-033 | 구조화출력 | LLM 어댑터별 구조화 출력 방식을 분기한다 | OpenAI: response_format(strict), Claude: prompt+tool trick, Ollama: format+prompt. 검증은 SchemaService.validate()로 후처리 | High |
| BIZ-034 | 구조화출력 | 구조화 출력 스키마 검증 실패 시 최대 2회 재시도한다 | 2회 재시도 후에도 실패 시 에러 반환. response_format 미지정 시 기존 텍스트 응답 유지 | High |
| BIZ-035 | 정책 | 정책 규칙은 6가지 타입으로 분류한다 | content_filter, cost_limit, token_limit, rate_limit, model_filter, time_restriction. 타입별 JSON 템플릿 자동 생성 | Medium |
| BIZ-036 | RAG | LLM Judge 평가 모드에서 Claude Haiku API로 3개 메트릭을 평가한다 | _llm_judge_faithfulness, _llm_judge_context_relevancy, _llm_judge_answer_relevancy. 평가당 LLM 3회 호출 | High |
| BIZ-037 | RAG | RAG 평가 mode가 "fast"이면 임베딩 유사도, "accurate"이면 LLM Judge로 평가한다 | fast=기존 RAGAS 방식, accurate=Claude Haiku 기반. 기본값 fast | Medium |
| BIZ-038 | 워크플로우 | WorkflowStep은 @JsonIgnoreProperties(ignoreUnknown=true)로 미지원 필드를 무시한다 | @JsonAlias("dependencies")로 dependsOn/dependencies 양쪽 키 허용. name 필드 추가(선택적, null 허용) | Medium |
| BIZ-039 | 워크플로우 | ConfigPanel은 StepType별 전용 설정 폼을 제공한다 | LLM_CALL, TOOL_CALL, CONDITION, PARALLEL, HUMAN_INPUT, ACTION 6가지 타입. LLM_CALL에 tool_choice, response_schema 포함 | High |
| BIZ-040 | 워크플로우 | ACTION 스텝의 payload에서 {{step.output}} 변수 치환을 지원한다 | 이전 스텝 출력을 참조하는 변수 구문. type은 WRITE\|NOTIFY, adapter/destination/payload 필드 구성 | Medium |
| BIZ-041 | 프로젝트 | 프로젝트는 회사(Tenant) 내 리소스 스코핑 단위이다 | Connection, Policy, User, Role은 회사 소속(현재 그대로). Knowledge Source, Prompt, Schema는 회사 소속 + 프로젝트에 N:M 할당. Workflow는 project_id nullable(있으면 프로젝트 전용, null이면 회사 공유) | High |
| BIZ-042 | 프로젝트 | X-Project-Id 헤더가 있으면 해당 프로젝트에 할당된 리소스 + 프로젝트 소속 Workflow만 반환한다 | 미지정 시 회사 전체 리소스 반환 (하위 호환). TenantContext 설정 후 ProjectContext 설정 순서 | High |
| BIZ-043 | 프로젝트 | 사용자는 소속 프로젝트의 리소스만 접근할 수 있다 | project_members 테이블로 관리. 역할: admin(전체), editor(생성/수정), viewer(조회). 프로젝트 미소속 사용자는 회사 공유 리소스만 접근 | High |
| BIZ-044 | MCP | MCP 관리 도구의 description에 워크플로우 작성 규칙을 포함한다 | StepType별 config 스펙, DAG 규칙(dependsOn, 순환참조 금지), 변수 바인딩({{input.key}}, {{stepId.field}}), 최대 루프 5회. 별도 가이드 문서 불필요 | High |
| BIZ-045 | MCP | MCP 관리 도구는 기존 REST API와 동일한 서비스를 호출한다 | MCP Tool → 내부 Service → Repository. 권한/검증 로직 동일 적용 | Medium |
| BIZ-046 | 압축 | Post-Compact 후 최근 참조 파일 5개 + 장기 메모리를 토큰 예산(50K) 내에서 자동 재주입한다 | ToolExecutionLog에서 최근 file_read/glob/grep top-5 추출. 파일당 5K 토큰 상한. LONG_TERM 메모리 + MCP 도구 스키마 재주입. [CR-031] | High |
| BIZ-047 | 압축 | MICRO_COMPACT는 LLM 호출 없이 마커 대체로 수행한다. 최근 3개 tool result는 보존 | 오래된 tool result → `[Tool result cleared]` 마커. 동일 hash 요약은 캐시. 기존 Haiku 요약은 캐시 미적중 시 폴백. [CR-031] | Medium |
| BIZ-048 | 메모리 | 대화 5턴 이상 완료 시 Haiku로 메모리 자동 추출, cosine similarity 0.9 이상이면 중복 판정하여 업데이트 | fire-and-forget 비동기. 4가지 타입: task-specific, person context, pattern, preferences. 턴 기반 throttle. [CR-031] | High |
| BIZ-049 | LLM | Adaptive Thinking은 Claude 4.6+ 모델에서만 지원. 미지원 모델은 ENABLED로 폴백 | thinking 파라미터: adaptive → `{"type":"adaptive"}`, enabled → `{"type":"enabled","budget_tokens":N}`, disabled → 미전송. [CR-031] | Medium |
| BIZ-050 | LLM | OpenAI 호환 shim은 Chat Completions API 호환 모든 서비스에 적용. base_url + api_key로 연결 | OpenAIOkHttpClient.builder().baseUrl(base_url) 사용. tool_choice/streaming 호환성은 서비스별 차이 허용. [CR-032] | High |
| BIZ-051 | 라우팅 | 에이전트별 라우팅에서 preferredConnectionId가 지정되면 ModelRouter를 우회한다 | SubagentRequest.preferredConnectionId → ConnectionAdapterFactory.getAdapter() 직접 호출. 미지정 시 기존 ModelRouter 경로 유지. [CR-032] | Medium |
| BIZ-052 | 에이전트사고 | Plan Mode에서는 읽기 전용 도구만 허용하며 쓰기/편집 도구를 자동 차단한다 | ToolFilterContext.readOnlyMode() 활성화. planModeActive=true 시 PermissionLevel.READ_ONLY 강제. 쓰기 도구 호출 → ToolResult.denied("Plan mode: read-only") 반환. [CR-033] | High |
| BIZ-053 | 에이전트사고 | 세션당 활성 Plan은 1개만 허용한다 | PLANNING 또는 EXECUTING 상태의 Plan이 있으면 EnterPlanModeTool 호출 거부. COMPLETED/ABANDONED 이후에만 새 Plan 생성 가능. [CR-033] | High |
| BIZ-054 | 에이전트사고 | Plan 상태 전이는 FSM을 따른다: PLANNING→EXECUTING→VERIFYING→COMPLETED | 허용 전이: PLANNING→EXECUTING(exit), PLANNING→ABANDONED(abandon), EXECUTING→VERIFYING(verify), EXECUTING→PLANNING(re-plan), VERIFYING→COMPLETED(100%), VERIFYING→EXECUTING(gaps). [CR-033] | High |
| BIZ-055 | 에이전트사고 | TodoWriteTool은 전체 교체 방식으로 동작한다 (incremental 아님) | 매 호출 시 todos 배열 전체를 받아 기존 데이터를 덮어쓴다. in_progress 상태는 동시에 1개만 권장(경고 메시지). 세션 종료 시 자동 소멸. [CR-033] | Medium |
| BIZ-056 | 에이전트사고 | 세션당 동시 실행 태스크(Task)는 최대 5개로 제한한다 | RUNNING 상태 태스크 5개 초과 시 TaskCreate 거부. Virtual Thread 기반이므로 OS 스레드 고갈 없음. SubagentRunEntity 재활용. [CR-033] | Medium |
| BIZ-066 | MCP | MCP 리소스 텍스트 읽기 시 32KB 초과 콘텐츠는 트렁케이션한다 | ReadMcpResourceTool에서 text 콘텐츠가 32,768자 초과 시 잘라내고 truncated=true 반환. blob은 제한 없음 (base64 반환). 에이전트 메시지 본문 32KB 제한(BIZ-057)과 정합. [CR-038] | Medium |
| BIZ-067 | 자동화 | RemoteTriggerTool은 테넌트당 분당 10회로 제한한다 | Redis 슬라이딩 윈도우 카운터 `trigger:rate:{tenantId}` TTL 60초. 초과 시 429 에러 반환. Cron 실행은 별도 카운터 (영향 없음). [CR-038] | Medium |
| BIZ-068 | 세션 | BriefTool 브리핑 캐시 TTL은 1시간이다 | session_briefs 테이블에 저장. created_at + 1시간 이내면 캐시 반환. force_refresh=true 시 캐시 무시. 세션 삭제 시 cascade 삭제. [CR-038] | Medium |
| BIZ-069 | 세션 | BriefTool 브리핑 생성 시 최근 50개 메시지만 사용한다 | SessionStore에서 최근 50개 메시지를 역순 추출. 메시지 50개 미만이면 전체 사용. 토큰 효율을 위해 Haiku급 모델로 요약. [CR-038] | Medium |
| BIZ-057 | 스케줄 | Cron 스케줄은 테넌트당 최대 50개로 제한한다 | CronScheduleManager.createJob()에서 검증. 50개 초과 시 IllegalStateException 반환. 비활성 작업도 카운트에 포함. [CR-035] | High |
| BIZ-058 | 스케줄 | Cron 최소 간격은 1분이다 (서버 부하 방지) | cron_expression 파싱 후 실행 간격 1분 미만이면 IllegalArgumentException 반환. 예: `* * * * * ?` (매초) 거부. [CR-035] | High |
| BIZ-059 | 스킬 | SkillInvokeTool은 활성화된(is_active=true) 스킬만 호출 가능하다 | 비활성 스킬 호출 시 ToolResult.denied() 반환. SkillRepository.findByIsActiveTrueOrderByNameAsc()로 필터링. [CR-035] | High |
| BIZ-060 | 도구검색 | ToolSearchTool 검색 결과는 최대 20개로 제한한다 | ToolRegistry에서 키워드/태그 매칭 결과를 relevance 정렬 후 상위 20개만 반환. [CR-035] | Medium |
| BIZ-061 | 웹파싱 | Firecrawl 실패 시 기본 HTTP 파싱으로 폴백한다 | Firecrawl API 호출 실패(타임아웃, 5xx) 시 Java HttpClient + Jsoup 기반 HTML 파싱으로 자동 전환. 폴백 시 마크다운 변환 품질 저하 허용. [CR-035] | Medium |
| BIZ-062 | 정책 | DOMAIN_FILTER 정책은 정규식 패턴을 지원한다 | rules.config.blocked_domains에 정규식 패턴 허용 (예: `.*\.gambling\.com`). Pattern.compile()로 컴파일, 유효하지 않은 정규식은 리터럴 매칭 폴백. [CR-035] | Medium |
| BIZ-063 | 정책 | 차단된 도메인 접근 시 PolicyEngine이 DENY를 반환한다 | DOMAIN_FILTER 규칙 매칭 시 즉시 PolicyDecision.DENY 반환. 감사 로그에 차단 URL + 매칭 패턴 기록. BIZ-005 정책 평가 순서 준수. [CR-035] | High |
| BIZ-064 | 스킬 | 스킬 삭제 시 참조 중인 워크플로우 확인이 필수이다 | 워크플로우 steps에서 해당 스킬을 참조하는 TOOL_CALL이 있으면 삭제 거부. 강제 삭제 옵션 미지원 (참조 해제 후 삭제). [CR-035] | High |
| BIZ-065 | 웹파싱 | Firecrawl 타임아웃은 30초, 최대 페이지 크기는 10MB이다 | 30초 내 응답 없으면 BIZ-061 폴백. 10MB 초과 응답은 트렁케이션. Content-Length 사전 검사 가능 시 다운로드 전 차단. [CR-035] | Medium |
| BIZ-070 | 프롬프트 | 프롬프트 조회 우선순위는 Redis 캐시 → DB → 파일 폴백 순이다 | PromptTemplateService에서 캐시 히트 시 즉시 반환. 캐시 미스 시 DB 조회 후 캐시 적재. DB에도 없으면 classpath 파일 폴백. [CR-036] | High |
| BIZ-071 | 프롬프트 | 카테고리 SYSTEM의 시스템 프롬프트(is_system=true)는 삭제 불가하다 | PromptTemplateController.delete()에서 is_system 검사. 삭제 시도 시 에러 반환 ("시스템 프롬프트는 삭제할 수 없습니다"). template 필드 수정은 허용. [CR-036] | High |
| BIZ-072 | 프롬프트 | 프롬프트 캐시 TTL은 5분이다 (Redis) | PromptTemplateService 캐시 키 패턴: `prompt:{key}:{version}`. 생성/수정/삭제 시 invalidateCache(key) 호출로 즉시 무효화. bulkLoad도 캐시 적용. [CR-036] | Medium |

---

## 명명 규칙

- 규칙 ID: `BIZ-[순번]` (3자리 zero-padding)
- 영역: 해당 규칙이 적용되는 도메인 모듈
- 우선순위: High = 시스템 무결성에 직접 영향, Medium = 기능 정확성에 영향

---

## 비즈니스 규칙 vs 정책 구분 기준

| 구분 | 비즈니스 규칙 (T1-3) | 정책 (T1-4) |
|------|---------------------|-------------|
| 변경 방법 | 코드 수정 | 설정/DB 변경 |
| 예시 | 도구 호출 최대 5회 | Rate Limit 윈도우 크기 |
| 저장 위치 | 소스코드 (하드코딩) | policies 테이블 / application.yml |
| 변경 빈도 | 거의 없음 | 운영 중 수시 변경 |
