# 원본 요구사항 — CR-060 위젯 음성 입력(STT)

**대화 일자**: 2026-04-24
**요청자**: sykim1850
**컨텍스트**: CR-061(위젯 파일 업로드) 완료 직후, 위젯 기능 확장 로드맵의 다음 항목으로 CR-060 착수

---

## 사용자 원문

> 🎯 다음 작업: CR-060 음성 입력 UI (STT)
> 왜 이것인가
>
> Python 사이드카에 STT 이미 구현됨 (Sprint 28, CR-011 Phase 3)
> 위젯 UI에 마이크 버튼 + 녹음 → STT API 연결만 하면 됨
> 파일 업로드(CR-061) 다음 자연스러운 위젯 확장
> 모바일 사용성 개선
> 시작 시 확인할 것
>
> docs/CR_변경_이력.md 에서 CR-060 등록 여부 (없으면 발번 필요)
> Python 사이드카 STT 엔드포인트 위치 (Sprint 28 산출물)
> CR-061에서 추가한 위젯 첨부 UI 패턴 재활용
> 메모리: project_cr061_status.md (방금 추가된 구조 참고)

---

## 착수 시 사실 정정

1. **STT 인프라 위치 정정**: 메모에 "Python 사이드카"라고 기재되었으나 실제 구현은 **BE 직접 프록시**다.
   - 위치: [SpeechController.java](../../backend/platform-core/src/main/java/com/platform/api/SpeechController.java) — `POST /api/v1/speech/stt`, `POST /api/v1/speech/tts`
   - OpenAI Whisper / TTS API 직접 프록시, 테넌트 OpenAI Connection 키 사용
   - Python 사이드카에는 STT 관련 코드 없음
2. **CR-060 발번 상태**: 정식 등록 안 됨. CR-058 § 9, CR-061 § 범위 경계에서 "후속 후보"로만 언급. 본 CR로 정식 발번 필요.

## 정석 여부 확인 교환 (2026-04-24)

최초 제안: 3MD 타협안 (기존 `/api/v1/speech/stt` 재사용 + toggle 녹음 + 임의 상수값)
사용자 질문: "추천안은 정석인가요? 아님 빨리할려고하는 추천인가요?"
재검토 결과: 타협 항목 6개 식별 (전용 엔드포인트 / 녹음 방식 / 상수값 런타임 설정화 등)
사용자 결정: **"무조건 정석으로 가세요"** → 4.5MD 정석안 확정

## 정석안 핵심 결정 (사용자 승인)

| # | 결정 |
|---|------|
| 1 | **전용 API**: `POST /api/v1/chat/stt` (위젯 경로). 내부 `SpeechService` 추출해 기존 `SpeechController`와 로직 공유 |
| 2 | **신규 스코프** `chat:stt` + `widget.allowed-scopes` 기본값 추가 |
| 3 | **녹음 방식**: 기본 toggle(클릭 시작/정지) + 길게 누름 push-to-talk 보조 |
| 4 | **오디오 포맷**: WebM/Opus 기본 + Safari 폴백 mp4/aac |
| 5 | **녹음 시간 제한**: `global_config` `widget.stt.max-duration-seconds` 기본 60 |
| 6 | **파일 크기 제한**: `global_config` `widget.stt.max-size-bytes` 기본 25MB (Whisper 한계) |
| 7 | **결과 처리**: 입력창 자동 삽입 (사용자가 편집·전송 결정) |
| 8 | **실시간 스트리밍**: 하지 않음 (Whisper 미지원) — 일괄 전송 |
| 9 | **언어**: `language=auto` 기본, `ko/en/...` 선택값 허용 |
| 10 | **BIZ 규칙 신설**: BIZ-102(녹음 시간) / BIZ-103(파일 크기) / BIZ-104(세션당 rate limit) |
| 11 | **감사 로그**: STT 호출 event 기록, **변환 텍스트 본문 저장 금지** (PII) |

## 범위 경계 (본 CR 제외)

- **실시간 스트리밍 STT** — Whisper 미지원 (Realtime API는 별도 CR 후보)
- **TTS 위젯 UI** — Assistant 응답 읽기 기능은 별도 CR
- **다중 화자 분리 / 타임스탬프 세그먼트** — Whisper 기능은 있으나 위젯 UI에서 미노출
- **오프라인/온디바이스 STT** — 전량 서버 경유

## 참고 재활용

- CR-061 5MD 구성 패턴 (Phase 1~5)
- `AttachmentController` 의 scope 검증 + Rate Limit 구조
- `widget.ts` 의 클립 버튼/칩 UI → 마이크 버튼/파형 타이머 패턴으로 적용
- `attachment-client.ts` 의 XHR 프로그레스 → `stt-client.ts` 에 그대로 이식

## 예상 산출물

- 설계서: `docs/T3-11_CR-060_STT_설계서.md`
- CR 카드: `docs/CR_변경_이력.md` § CR-061 아래 신설
- Plan: `~/.claude/plans/cr-060-stt-widget.md`
- 메모리: `project_cr060_status.md`
- PRD 신규: 324~326 (3개) — `/chat/stt` 엔드포인트 / 위젯 마이크 UI / 런타임 설정
- FE 신규: 037
- BIZ 신규: 102~104 (3개)
- Flyway: master V19 — `widget.stt.*` global_config seed (신규 테이블 없음)

## 참조 문서

- CR-058 Chat Widget SDK 설계서 (`docs/T3-9_CR-058_ChatWidget_설계서.md`)
- CR-061 File Upload 설계서 (`docs/T3-10_CR-061_FileUpload_설계서.md`)
- 기존 Speech 프록시 (`backend/.../api/SpeechController.java`)
