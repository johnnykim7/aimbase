# CR-124 원본 — MCP SDK 2.0.0 업그레이드 (프로토콜 2025-11-25 협상 실패 해소)

작성일: 2026-07-21
발단 세션: bp-wes 원격 도구 CLI 노출 작업(CR-121/122/123) 후속으로 발견

---

## 1. 발단 — 어떻게 발견됐나

bp-wes 소비앱이 등록한 원격 도구 8종(`get_progress`, `get_bottleneck` 등)을 Claude CLI 러너가
호출하지 못하는 문제를 추적하는 과정에서 드러났다. 앞선 3건을 차례로 해소했는데도 증상이 남았다.

| 순번 | 해소한 문제 | 결과 |
|---|---|---|
| CR-121 | 원격 도구가 `/mcp/sse` 에 노출되지 않음 (Spring 빈 vs ToolRegistry) | 노출 110개 확인 |
| CR-122 | 테넌트 DataSource 기동 로드 실패 (BCrypt 저장↔평문 사용) | 테넌트 7/7 로드 |
| CR-123 | 커넥션 config 변경이 어댑터 캐시에 반영 안 됨 | `toolMode=AIMBASE` 전파 확인 |
| **CR-124** | **MCP 프로토콜 버전 협상 실패** | **미해결 — 본 CR** |

CR-123 까지 끝내 `toolMode=AIMBASE` 로 워커가 뜨는 것까지 확인했으나, CLI 의 MCP 상태가
`status=pending` 에서 고정되고 도구가 0개인 증상이 남았다.

---

## 2. 근본 원인 (운영 로그 실측 확정)

aimbase BE 로그:
```
INFO  i.m.server.McpAsyncServer : Client initialize request - Protocol: 2025-11-25
WARN  i.m.server.McpAsyncServer : Client requested unsupported protocol version: 2025-11-25,
                                  so the server will suggest the 2024-11-05 version instead
```

- **클라이언트**: Claude Code CLI 2.1.197 → `2025-11-25` 요구
- **서버**: aimbase, MCP Java SDK **0.17.0** → `2024-11-05` / `2025-03-26` / `2025-06-18` 만 지원
  (`McpSchema.LATEST_PROTOCOL_VERSION = "2025-06-18"` — jar 디컴파일 실측)
- 서버가 `2024-11-05` 를 역제안 → CLI 가 수용하지 않음 → 핸드셰이크 미완료

CLI 요청 5회 전부 동일하게 재현되는 **결정적 실패**(간헐 아님).

### 서버는 정상임을 증명한 대조 실험

러너 컨테이너 안에서 러너와 **동일한 키·헤더**로 수동 프로브(프로토콜만 `2024-11-05` 명시):

```
event:endpoint  → data:/mcp/message?sessionId=...        (SSE 정상)
initialize      → HTTP 200 + result 정상 수신
tools/list      → 도구 110개 반환 (get_progress / get_bottleneck / get_current_time 포함)
```

즉 **SSE·인증·도구 노출·CR-121 수정 전부 정상**이며, 실패 지점은 프로토콜 버전 협상 단 하나다.

---

## 3. 영향 범위 — 소비앱 문제가 아니다

- bp-wes 가 만든 도구/등록 방식과 무관. 소비앱 코드로는 해결 불가
- **AIMBASE 모드를 쓰는 순간 어느 소비앱이든 동일하게 발생**한다
- 지금까지 표면화되지 않은 이유: 다른 러너(bidding/axopm/workmap)가 전부 **NATIVE 모드**라
  MCP 연결 자체를 시도하지 않았다(`mcp_servers=[]`). bp-wes 가 처음 AIMBASE 를 써서 드러났다
- **현재 aimbase 는 AIMBASE 모드가 사실상 동작 불가 상태** (어느 러너든 MCP 도구 0개)

---

## 4. 해결 경로 — SDK 2.0.0 이 유일

외부 조사 결과:

- MCP Java SDK **2.0.0** 이 `2025-11-25` 스펙을 지원 (모든 transport 에 추가)
- **1.x 대는 미지원** — 1.1.3 도 `2024-11-05/2025-03-26/2025-06-18` 만 지원
  ([issue #715](https://github.com/modelcontextprotocol/java-sdk/issues/715))
- 따라서 0.17.0 → **2.0.0** 직행 외에 선택지가 없다

### 2.0.0 주요 Breaking Change (릴리스 노트)

| 변경 | aimbase 영향 |
|---|---|
| **서버측 SSE transport deprecated** (Streamable HTTP 권장) | `WebMvcSseServerTransportProvider` — **현재 사용 중**, 최대 리스크 |
| `JsonSchema` → `Map` (inputSchema) | `McpToolConversion` |
| sealed interface 제거 (JSONRPCMessage/Request/Result) | 패턴매칭 코드 |
| lenient deserialization | 하위호환 개선(위험 낮음) |

---

## 5. 영향 파일 (실측)

### 의존성 선언 2곳
```
backend/platform-core/build.gradle.kts:32     mcp-bom:0.17.0
backend/sdk/tool-sdk-mcp/build.gradle.kts:9   mcp-bom:0.17.0
```
아티팩트: `mcp`, `mcp-spring-webmvc`, `mcp-json-jackson2`

### `io.modelcontextprotocol` 를 import 하는 소스 8개
```
platform-core/src/main/java/com/platform/mcp/server/ServerMcpConfig.java          ★ SSE transport + McpSyncServer
platform-core/src/main/java/com/platform/mcp/server/ServerMcpToolDispatcher.java     McpSchema.CallToolResult/Content
platform-core/src/main/java/com/platform/mcp/admin/AimbaseAdminMcpConfig.java     ★ /admin-mcp/sse (14 tools)
platform-core/src/main/java/com/platform/mcp/MCPServerClient.java                    클라이언트 측
sdk/tool-sdk-mcp/src/main/java/com/platform/mcp/agent/AgentMcpServer.java         ★ SSE transport (agent 측)
sdk/tool-sdk-mcp/src/main/java/com/platform/mcp/agent/McpToolConversion.java         inputSchema 변환
platform-core/src/test/java/com/platform/mcp/server/ServerMcpToolDispatcherTest.java
sdk/tool-sdk-mcp/src/test/java/com/platform/mcp/agent/AgentMcpServerTest.java
```
★ = 서버측 SSE transport 사용처(deprecated 대상, 전환 판단 필요)

**주의**: `ServerMcpConfig` 는 CR-121 로 방금 수정한 파일이다(주기 동기화 + 기동 순서 가드).
2.0.0 전환 시 그 로직(`addTool`/`removeTool`/`notifyToolsListChanged`)이 새 API 로 살아남아야 한다.

---

## 6. 선행 확인 사항 (다음 세션에서 먼저 답할 것)

1. **2.0.0 에서 서버측 SSE 를 계속 쓸 수 있나?** deprecated 이지 제거는 아닐 가능성 —
   제거됐다면 Streamable HTTP 전환이 필수가 되어 규모가 크게 달라진다
2. **Claude CLI 가 Streamable HTTP 를 지원하나?** `--mcp-config` 의 `"type":"sse"` 를
   어떤 값으로 바꿔야 하는지 (CLI 문서/실측)
3. **`addTool`/`removeTool`/`notifyToolsListChanged` API 가 2.0.0 에 남아있나** (CR-121 의존)
4. Spring AI 와의 버전 정합성 (`mcp-spring-webmvc` 호환 범위)
5. agent 측(`tool-sdk-mcp`)과 BE 측을 **동시에** 올려야 하는지, 단계 배포가 가능한지

---

## 7. 참고 — 선례

- `project_mcp_sdk_upgrade_pending` (메모리): 0.10.0 → 0.17.0 업그레이드로 첫 호출 180초 hang 해소.
  같은 계열 작업이나 이번은 major 버전 점프라 규모가 더 크다
- `feedback_mcp_sse_url_split`: SDK 0.10.0 `baseUri.resolve("/sse")` 함정 — transport 변경 시 재확인
- `feedback_mcp_sse_180s_hang`: 죽은 세션 404 미전파

---

## 8. 당장의 우회 (본 CR 완료 전까지)

- 소비앱은 **NATIVE 모드 + push 방식**으로 운영 (bp-wes 세션에서 push 폴백 이미 구현)
- AIMBASE 모드(능동조회/pull)는 본 CR 완료 후 재개

---

## Sources
- https://github.com/modelcontextprotocol/java-sdk/releases
- https://github.com/modelcontextprotocol/java-sdk/issues/715
- https://central.sonatype.com/artifact/io.modelcontextprotocol.sdk/mcp-core
