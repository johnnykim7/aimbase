{
  "type": "ACTION",
  "connector": "aimbase-api",
  "action": {
    "method": "POST",
    "endpoint": "/connections/{{steps.aimbase.connection.create.output.connectionId}}/test",
    "headers": {}
  },
  "extract": {
    "ok": "$.body.data.ok",
    "latencyMs": "$.body.data.latencyMs"
  },
  "assert": [
    { "field": "$.body.success", "operator": "==", "value": true },
    { "field": "$.body.data.ok", "operator": "==", "value": true }
  ],
  "failMode": "hard"
}
