{
  "type": "PROBE_DB",
  "connector": "aimbase-master-db",
  "name": "테넌트 삭제 후 DB 제거 확인",
  "action": {
    "query": "SELECT count(*) as cnt FROM pg_database WHERE datname = '{{vars.deletedTenantDb}}'"
  },
  "assert": [
    { "field": "$.rows[0].cnt", "operator": "==", "value": 0 }
  ],
  "failMode": "hard"
}
